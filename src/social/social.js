/*
const danishSsn = require('danish-ssn')
const amka = require('greece-amka')

const Validator13 = require('ahv13-validator')
const ahv13 = new Validator13()

import SSN from "french-ssn"

danishSsn.isValid(value)
SSN.validate(value)
amka.validate(value)
ahv13.isValid('756.9217.0769.85')

const getSex = (value) => {
  danishSsn(value).sex // "Female"
  SSN.parse(value).gender.name // "female"
}

const czechAndSlovakNationalIDisFemale = function (value) {
  let { rodnecislo } = require('rodnecislo')
  let rc = rodnecislo(value)
  return rc.isFemale()
}

const validate = require('nir_validate') // france

//const Verifications = require('verifications')
//Verifications.NationalID.verify(value, 'US')

const validate = require('swe-validation') // ...
*/

// AMKA -> GR

/*
const EstonianGetGender = function (value) {
  const ik = new Isikukood('47502124911')
  const isValid = ik.validate()
  return ik.getGender() // "male" or "female"
}
*/
